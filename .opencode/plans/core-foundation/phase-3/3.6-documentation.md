# 3.6. Documentation Updates

**Goal:** Update project docs to reflect Phase 3 changes.

## Sources

| Source file                         | Content                                 |
| ----------------------------------- | --------------------------------------- |
| `AGENTS.md`                         | Project info, commands, gotchas         |
| `docs/CODEMAPS/ARCHITECTURE.md`     | Layer diagram                           |
| `docs/CODEMAPS/MODULES.md`          | Per-module descriptions                 |
| `docs/CODEMAPS/FILES.md`            | Directory tree                          |
| `docs/integrations/layer-wiring.md` | Full layer integration guide, E2E trace |

## Tasks

### 3.6.1. Update `AGENTS.md`

- [x] Add `@repo/presenters` to package layout table
- [ ] Add `@repo/generator` to package layout table (blocked on 3.4)
- [x] Add `white-label-vue` app name
- [ ] Add `generate:vue-tenant` command to commands table (blocked on 3.4)
- [x] Update architecture description: `domain → infra → presenters → apps/white-label-* → apps/tenant-*`
- [ ] Add tenant creation section (blocked on 3.4)

### 3.6.2. Update `ARCHITECTURE.md`

- [x] Add presenter layer to diagram
- [x] Add white-label → tenant layer extends
- [x] Add `@repo/presenters` to package diagram
- [ ] Add `@repo/generator` to package diagram (blocked on 3.4)
- [x] Update data flow arrows

### 3.6.3. Update `MODULES.md`

- [x] Add `@repo/presenters` — description, exports, dependencies
- [ ] Add `@repo/generator` — description, generators, usage (blocked on 3.4)
- [x] Update white-label app description (replaces web-vue)
- [x] Add fake-plants-vue app description (example tenant)

### 3.6.4. Update `FILES.md`

- [x] Add `packages/presenters/` to dir tree
- [ ] Add `packages/generator/` to dir tree (blocked on 3.4)
- [x] Replace `apps/web-vue/` with `apps/white-label-vue/` in tree
- [x] Add `apps/fake-plants-vue/` to tree
- [ ] Add `apps/tenant-<name>-vue/` pattern note (blocked on 3.4)

### 3.6.6. Additional fixes (post-hoc)

- [x] Fix `@repo/white-label-vue` → `white-label-vue` across all docs (package name has no `@repo` scope)
- [x] Fix ProductView `logo`/`logoFamily` → `image`/`imageFamily` to match actual code
- [x] Add `metaMap`/`ProductMeta` to all doc examples (metadata injection pattern)
- [x] Add `x-tenant-id` header to infra adapter doc
- [x] Add `VITE_TENANT_ID` env var to docs
- [x] Update E2E trace for metadata enrichment flow

### 3.6.5. Tenant creation guide (blocked on 3.4)

- [ ] Create or update docs with: (blocked on 3.4)
  - Prerequisites (white-label exists, generator installed)
  - `bun run generate:vue-tenant --name <tenant>`
  - Manual override patterns (components, pages, styles)
  - Build + dev instructions

## ✅ Manual Confirmation

- [ ] Human reads updated docs — accurate and complete
- [ ] Human confirms: "3.6 complete, Phase 3 done"

## After Phase 3

- [x] Update `.opencode/plans/core-foundation/README.md` status: Phase 3 ✅ COMPLETED
- [x] Update `.opencode/plans/core-foundation/phase-3.md` status: ✅ COMPLETED
