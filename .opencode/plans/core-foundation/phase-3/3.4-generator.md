# 3.4. `@repo/generator` + Vue Tenant Pinion Generator

**Goal:** Create a reusable Pinion-based code generator that scaffolds tenant apps from the validated fake-plants-vue pattern.

## Sources

| Source file             | Content                                       |
| ----------------------- | --------------------------------------------- |
| `package.json` (root)   | Add devDep + npm script                       |
| `apps/fake-plants-vue/` | Reference implementation for generator output |
| `packages/generator/`   | Utility package to create                     |
| `@featherscloud/pinion` | TS-native code generator library              |

## Design Decisions (2026-06-30)

Agreed generator design from architecture discussion:

### Metadata

- Generator asks ONCE about metadata generation mode
- Scaffolds 2-3 placeholder entries in a `metadata.ts` file with commented examples
- Mode determines: fixture-based (static product map) vs real API integration pattern

### Tokens / Theming

- **Removed palette calculation** — WA theme system provides all derived brand colors
- 4 theme choices at gen time: `default` | `awesome` | `shoelace` | `custom`
- Predefined themes: import WA theme CSS, no tokens.css brand overrides needed
- Custom: validate hex → set single `--wa-color-brand-fill-normal` var, WA cascade handles rest
- `packages/generator/src/utils/palette.ts` reduced to `validateHex()` + `getThemeClass()` only (no HSL math)

- Prompt for 1 primary brand CSS variable (e.g. `--brand-hue` or a primary color hex)
- Generator calculates a palette with HSL values from the primary input
- Templates `src/styles/tokens.css` with calculated palette + commented placeholders for remaining brand vars
- Generated file includes `@wa-theme-default` class and `--wa-color-*` var overrides

### Component override

- OPTIONAL prompt: "Override a component? [y/N]"
- If yes: ask for component name, scaffold a stub in `src/components/<Name>.vue`
- Ensure `vite.config.ts` includes `componentDirs: ['./src/components']`
- Only component override — page override skipped in v1

### Page override

- **Skipped in v1** (pencilled for future phase)

### New routes

- **Skipped explicit scaffold in v1**
- Add a **COMMENTED example** in generated `main.ts` showing how to use `extendRoutes`

### Updated factory API (target state)

- `createWhiteLabelApp` now accepts `extendRoutes?: RouteRecordRaw[]` in addition to `routes`
- When `extendRoutes` is provided, WL default routes merge automatically — tenant doesn't repeat them
- Generated tenant `main.ts` uses `extendRoutes` (simpler pattern)
- `routes` kept as full override for advanced use (backwards compat)
- Generator assumes factory API has been updated before generator is built

## Tasks

### 3.4.0. ✅ Precondition: Update factory API

- [x] Add `extendRoutes?: RouteRecordRaw[]` and `omitRoutePaths?: string[]` to `WhiteLabelAppOptions` in `apps/white-label-vue/src/bootstrap/init.ts`
- [x] Implement merge logic in `bootstrap/init.ts`: `mergeRoutes()` merges `extendRoutes` with WL defaults; `routes` used as full override (backwards compat); `omitRoutePaths` filters merged result
- [x] Refactored factory into bootstrap layer (`bootstrap/app.ts` + `bootstrap/init.ts`) — `createWhiteLabelApp` now delegates to `useWhiteLabelApp()`
- [x] Updated `white-label-vue/app` export to `./src/bootstrap/app.ts`
- [x] Verified fake-plants-vue works with `routes` (no breaking change) + now uses `extendRoutes` + `omitRoutePaths`


## Unplanned Changes

Changes that occurred during execution not described in original plan:

- **Bootstrap layer extraction**: Factory code was split from a single `src/app.ts` into `src/bootstrap/app.ts` + `src/bootstrap/init.ts`. `createWhiteLabelApp()` in `app.ts` delegates to `useWhiteLabelApp()` from `init.ts` for MSW setup, shell resolution, route merging, and meta injection. This cleanly separates concerns and makes testing easier.

## ➕ Phase 3.4 Extension: MSW data extraction + registration (2026-07-02)

**Decision:** Generator will optionally register the new tenant in MSW mock data. To make this safe,
we first refactor the inline `PRODUCT_TENANT_CONFIGS` from TS to a JSON file that scripts can read/write.

### Architecture

```
packages/infra/src/mocks/
  tenant-configs.json          ← NEW: JSON data, generator script writes to it
  mocked-data.ts               ← REFACTOR: imports JSON instead of inline definition
  mocked-data.spec.ts          ← UPDATE: adapt tests for JSON import
factories/product.ts           ← NO CHANGE: imports PRODUCT_TENANT_CONFIGS same way

root/scripts/
  register-msw-tenant.ts       ← NEW: bun script, reads/writes tenant-configs.json
```

### Prompts

Generator adds one optional question:
- "Register MSW mocked data for this tenant? [Y/n]" → boolean ctx.registerMsw

### Generator flow (updated)

```
.then(prompt(...))         // existing prompts + new registerMsw question
.then(caseTransform())     // derive Name, camelName, prefix
.then(renderSetup)         // package.json, configs (extracted)
.then(renderSourceFiles)   // main.ts, styles
.then(renderConditional)   // metadata (when), component/gitkeep (when)
.then(renderTestInfra)     // vitest.setup, app.spec, env, env.example
.then((ctx) => {
  if (ctx.registerMsw) {
    return exec(`bun root/scripts/register-msw-tenant.ts`, [ctx.prefix])(ctx)
  }
  ctx.pinion.logger.notice(`To register MSW data, run:
  bun scripts/register-msw-tenant.ts ${ctx.prefix}
  This adds an entry to packages/infra/src/mocks/tenant-configs.json`)
  return ctx
})
```

## Tasks

### 3.4.1. ✅ Precondition: Extract MSW configs to JSON

- [x] Add `"resolveJsonModule": true` to `packages/infra/tsconfig.json`
- [x] Create `packages/infra/src/mocks/data/mocked-data.json` with current `wl` + `fp` entries
- [x] Refactor `mocked-data.ts`:
  - Import JSON: `import tenantConfigs from './data/mocked-data.json'`
  - `export const PRODUCT_TENANT_CONFIGS = Object.freeze(tenantConfigs)`
  - Keep `FRAMEWORK_NAMES` / `FAKE_PLANTS_NAMES` as re-exports from JSON import
- [x] Update `mocked-data.json` 
  - Frozen test kept — `Object.freeze()` applied after import, test still works 
- [x] Verify: `bun run test` — infra tests pass, `bunx tsc --noEmit` — 0 errors

### 3.4.2. ✅ Create register-msw-tenant script

- [x] Create `scripts/register-msw-tenant.ts`:
  - CLI args: `--prefix <str> --names-json <JSON array>`
  - Reads `packages/infra/src/mocks/data/mocked-data.json`
  - Appends new entry via `addTenantConfig()` from `packages/generator/src/msw/add-tenant.ts`
  - Validates prefix doesn't already exist (error if duplicate)
  - Writes back with `JSON.stringify(data, null, 2) + '\n'`
- [x] Add root `package.json` script: `"register-tenant": "bun scripts/register-msw-tenant.ts"`

### 3.4.3. ✅ Modularize generator (A+B)

- [x] Extract sub-generator functions in `vue-tenant.tpl.ts`:
  - `renderSetup` — package.json, vite.config.ts, tsconfig.json, index.html
  - `renderSourceFiles` — main.ts, tokens.css, styles/index.ts
  - `renderConditionalAssets` — metadata.ts (when fixture), component (when override), gitkeep (else)
  - `renderTestInfra` — gitkeep pages, vitest.setup.ts, App.spec.ts, .env, .env.example
- [x] Update `generate()` to use sub-fns + `mswRegistration` step
- [x] Export all sub-fns for testing
- [x] Import `when` from `@featherscloud/pinion`

### 3.4.4. ✅ Wire MSW registration into generator

- [x] Add `registerMsw` question to prompts (default: true)
- [x] Create `mswRegistration` sub-fn — uses direct `readFileSync`/`writeFileSync` with `addTenantConfig()` (no `exec` call — simpler approach)
- [x] Prints instructions when `registerMsw` is false: "To register MSW data, run: bun scripts/register-msw-tenant.ts --prefix <name>"

### 3.4.5. ✅ Test sub-generator functions

- [x] Create `vue-tenant.tpl.spec.ts` — orchestration tests
- [x] Create `cases.spec.ts` — case transform tests
- [x] Create `palette.spec.ts` — hex validation tests
- [x] Create `templates/index.spec.ts` — template rendering tests
- [x] Create `prompts/index.spec.ts` — prompt shape tests
- [x] Create `msw/add-tenant.spec.ts` — MSW config add tests

<!-- 3.4.6 implemented below -->

### 3.4.6. ✅ Unified create-tenant orchestrator (2026-07-21)

**Goal:** Single entry point `bun run create-tenant` that scaffolds + installs deps + registers MSW + verifies.

**Architecture decision:** Extend pinion template chain (Option A from discussion). No new CLI script — add `installDeps` + `verifyScaffold` steps to `vue-tenant.tpl.ts`.

#### Design

```
bun run create-tenant
  → bun packages/generator/src/generators/vue-tenant.tpl.ts
  → prompt(tenantPrompts)     // interactive (name, theme, metadata, MSW, override)
  → caseTransform()
  → renderSetup               // package.json, configs
  → renderSourceFiles          // main.ts, styles
  → renderConditionalAssets    // metadata, component/gitkeep
  → renderTestInfra            // vitest.setup, env
  → mswRegistration            // add MSW config + copy worker (when registerMsw: true)
  → installDeps                // NEW: ctx.pinion.exec('bun', ['install']) in app dir
  → verifyScaffold             // NEW: check key files exist, warn on missing worker
```

#### Files changed

| File | Change |
|---|---|
| `packages/generator/src/generators/vue-tenant.tpl.ts` | Add `installDeps` + `verifyScaffold` steps to `generate` chain |
| `packages/generator/src/msw/register-tenant.ts` | NEW — moved from `scripts/`, same logic, relative imports |
| `packages/generator/package.json` | Add `@types/node`, `bun-types` devDeps |
| `scripts/register-msw-tenant.ts` | REMOVED (moved into generator pkg) |
| `scripts/tsconfig.json` | REMOVED (no longer needed — scripts/ has no TS files) |
| `root/package.json` | Update `register-tenant` path; add `create-tenant` cmd |
| `packages/generator/src/generators/vue-tenant.tpl.spec.ts` | Update mock context for new steps |

#### Rationale

- KISSME: 2 new functions (~10 lines each), zero new infra or files
- SRP: pinion template handles full orchestration; register-tenant.ts stays as thin CLI for data-only operation
- Testable: new steps are pure functions on context, mockable in existing test suite
- No new dependencies: `ctx.pinion.exec()` uses pinion's built-in exec (child_process)

#### Tasks

- [ ] Move `scripts/register-msw-tenant.ts` → `packages/generator/src/msw/register-tenant.ts`
- [ ] Add `installDeps` + `verifyScaffold` to `vue-tenant.tpl.ts`
- [ ] Update root `package.json` — `register-tenant` path + `create-tenant` alias
- [ ] Update `vue-tenant.tpl.spec.ts` mock context
- [ ] Remove `scripts/tsconfig.json` (no more TS files in scripts/)
- [ ] Run full test suite — 0 failures
- [ ] Typecheck — `npx tsc --noEmit` files in generator pkg

### 3.4.7. ⬜ Final verification (not yet run)

- [ ] `bunx tsc --noEmit` — 0 errors across all packages
- [ ] `bun run lint` — passes
- [ ] `bun run test` — all tests pass

## ✅ Manual Confirmation

- [ ] Human inspects generated tenant structure
- [ ] Human confirms: "Generator works, proceed to 3.5"

## Future factory improvements (pencilled)

Not scheduled for this phase — ideas recorded for future consideration:

- **Component/page override via Vite plugin resolution order**: Currently implicit and not factory-aware. If multiple tenants override the same component, resolution order is fragile. A future factory API could expose explicit override registration.
- **App shell override is all-or-nothing**: No slot-based layout extension from WL. Tenant must replace entire `App.vue` to change shell structure. Future: define layout regions (header, main, footer, sidebar) that WL shell exposes for tenant injection without full replacement.
- **No slot system for injecting content into WL layout regions**: Tenants cannot inject nav items, toolbar actions, or sidebar content into WL layouts without forking the shell. Future: a slot/region registry API.
- **CSS/token imports managed outside factory**: Tenants manually import `@repo/ui/styles`, theme, and tokens. Future: factory could auto-register theme dependency or accept a theme option.
- **Any other factory API improvements** discovered during tenant development or generator use.
