# 3.1. `@repo/presenters` — Domain→ViewModel Transformations

**Goal:** Extract presenter logic from `apps/web-vue/src/presenters/` into a shared, framework-agnostic `packages/presenters/` package.

## Sources

| Source file                                             | Content                                                                     |
| ------------------------------------------------------- | --------------------------------------------------------------------------- |
| `apps/web-vue/src/presenters/product.presenter.ts`      | Existing `ProductView` interface + `toProductView()`, `toProductViewList()` |
| `apps/web-vue/src/presenters/product.presenter.spec.ts` | 9 test cases (194 lines), ~95% coverage                                     |
| `packages/domain/src/models/Product.ts`                 | Domain model consumed by presenter                                          |
| `packages/domain/package.json`                          | Format reference for new package                                            |

## Tasks

### 3.1.1. Scaffold `packages/presenters/` package

- [x] Create `packages/presenters/package.json`
  - `name: "@repo/presenters"`, `private: true`, `type: "module"`
  - `exports: { ".": "./src/index.ts" }`
  - scripts: `lint`, `test`
  - devDeps: `@repo/eslint-config`, `@repo/typescript-config`, `typescript`, `vitest`, `eslint`
- [x] Create `packages/presenters/tsconfig.json`
  - extends `@repo/typescript-config/base.json`
  - `include: ["src"]`
- [x] Create `packages/presenters/.eslintrc.cjs` — extends `@repo/eslint-config/index.js`
- [x] Create `packages/presenters/vitest.config.ts`
  - `import { defineConfig } from 'vitest/config'`
  - Environment: jsdom (for DOM types if needed by presenters)
  - Include: `src/**/*.spec.ts`

### 3.1.2. Move `ProductView` + transformation functions

- [x] Create `packages/presenters/src/product.presenter.ts`
  - Export `ProductView` interface (from web-vue version)
  - Export `toProductView(product: Product): ProductView`
  - Export `toProductViewList(products: Product[]): ProductView[]`
  - Export internal helper `FrameworkMeta` interface + `frameworkMap` + `getFrameworkMeta()`
  - Import `type { Product } from '@repo/domain'`
  - No Vue/framework imports — pure TS only

### 3.1.3. Move tests

- [x] Create `packages/presenters/src/product.presenter.spec.ts`
  - Copy all 9 test cases from web-vue version
  - Import from local `./product.presenter.ts` (not `@repo/presenters` in tests)
- [x] Run `vitest run` in `packages/presenters/` — verify all pass
- [x] Verify coverage ≥ 80%

### 3.1.4. Barrel export

- [x] Create `packages/presenters/src/index.ts` — `export * from './product.presenter'`

### 3.1.5. Update web-vue imports

- [x] `apps/web-vue/src/pages/ProductsPage.vue` — change import from `'../presenters/product.presenter'` to `'@repo/presenters'`
- [x] `apps/web-vue/src/pages/ProductsPage.spec.ts` — same change

### 3.1.6. Remove old presenter directory

- [x] Delete `apps/web-vue/src/presenters/` recursively

### 3.1.7. Verify

- [x] `bun run build` — no type errors
- [x] `bun run test` — all tests pass
- [x] `bun run lint` — no lint errors

## ✅ Manual Confirmation

- [x] Run `bun run dev` in `apps/web-vue` — products page renders correctly with data from `@repo/presenters`
- [x] Human confirms: "3.1 complete, proceed to 3.2"
