# 3.5. Integration Improvements & Turborepo Pipeline

**Goal:** Final integration improvements for tenant apps — custom image component with error handling, navigation/data loaders, and pipeline configuration.

## Sources

| Source file                                    | Content                  |
| ---------------------------------------------- | ------------------------ |
| `turbo.json`                                   | Pipeline config          |
| `package.json` workspaces                      | `apps/*` glob            |
| `packages/ui/`                                 | fe-* WC wrappers         |
| `apps/fake-plants-vue/src/components/`         | Tenant components        |

## Pre-flight Checks (completed)

The following pipeline verification tasks were completed:

- ✅ `package.json` workspaces: `["apps/*", "packages/*"]` — already covers `apps/fake-plants-vue/` and `apps/*-vue` automatically
- ✅ No turbo.json change needed — Turborepo auto-discovers workspaces
- ✅ `fake-plants-vue` has `white-label-vue` in deps → turbo resolves `^build` dep chain automatically
- ✅ Chain: `@repo/domain` → `@repo/infra` → `@repo/ui` → `@repo/presenters` → `white-label-vue` → `fake-plants-vue`
- ✅ `bun run build` from root succeeds
- ✅ `bun run test` from root succeeds

## Tasks

### 3.5.1. Create custom `fe-img` component with fallback image

**Problem:** `ProductCard.vue` uses raw `<img>` with `Math.random()` URL → requests new image on every render. No error handling or default fallback.

- [ ] Create `packages/ui/components/fe-img.ts` — custom `fe-img` WC wrapping native `<img>`
- [ ] Implement `onerror` handler rendering default/fallback image when src fails
- [ ] Add stable image URL caching (avoid re-request on every render — e.g., deterministic seed per product)
- [ ] Register in `packages/ui/package.json` exports map
- [ ] Update `apps/fake-plants-vue/src/components/ProductCard.vue` to use `<fe-img>` instead of raw `<img>`
- [ ] Write tests for fe-img (src loading state, error fallback, attribute reflection)

### 3.5.2. Add loader components

**Problem:** No loading state indicators during route transitions or async data fetches.

- [ ] Create route navigation loader component (shown during async route transitions / lazy page loads)
- [ ] Create data loading loader component (shown while API calls / async composables are in-flight)
- [ ] Decide component location: inside `packages/ui/` as `fe-loader` or in `apps/white-label-vue/src/components/` as Vue SFC
- [ ] Integrate loaders into white-label app shell / layout
- [ ] Ensure WA styling compatibility (use `--wa-*` design tokens)
- [ ] Write tests

### 3.5.3. Improve fake-plants-vue Turborepo pipeline

- [ ] Audit `turbo.json` for tenant-specific cache configuration
- [ ] If tenant build differs from standard, add explicit pipeline task
- [ ] Verify parallel task execution works for tenant build chain
- [ ] Add `.turbo` to tenant `.gitignore` if missing

## ✅ Manual Confirmation

- [ ] `bun run build` from root succeeds
- [ ] `bun run test` from root succeeds
- [ ] `cd apps/fake-plants-vue && bun run dev` — loaders visible during navigation
- [ ] `cd apps/fake-plants-vue && bun run dev` — images show fallback on broken src
