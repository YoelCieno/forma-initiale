# 3.5. Integration Improvements & Turborepo Pipeline

**Goal:** Final integration improvements for tenant apps — custom image component with error handling, navigation/data loaders, and pipeline configuration.

## Sources

| Source file                                    | Content                  |
| ---------------------------------------------- | ------------------------ |
| `turbo.json`                                   | Pipeline config          |
| `package.json` workspaces                      | `apps/*` glob            |
| `packages/ui/`                                 | fe-* WC wrappers         |
| `apps/fake-plants-vue/src/components/`         | Tenant components        |

## Pre-flight Checks (completed)

The following pipeline verification tasks were completed:

- ✅ `package.json` workspaces: `["apps/*", "packages/*"]` — already covers `apps/fake-plants-vue/` and `apps/*-vue` automatically
- ✅ No turbo.json change needed — Turborepo auto-discovers workspaces
- ✅ `fake-plants-vue` has `white-label-vue` in deps → turbo resolves `^build` dep chain automatically
- ✅ Chain: `@repo/domain` → `@repo/infra` → `@repo/ui` → `@repo/presenters` → `white-label-vue` → `fake-plants-vue`
- ✅ `bun run build` from root succeeds
- ✅ `bun run test` from root succeeds

## Tasks

### 3.5.1. Create custom `fe-img` component with fallback image

**Problem:** `ProductCard.vue` uses raw `<img>` with `Math.random()` URL → requests new image on every render. No error handling or default fallback.

- [x] Create `packages/ui/components/fe-img.ts` — custom `fe-img` WC wrapping native `<img>`
- [x] Implement `onerror` handler rendering default/fallback image when src fails
- [x] Add stable image URL caching (avoid re-request on every render — e.g., deterministic seed per product)
- [x] Register in `packages/ui/package.json` exports map
- [x] Update `apps/fake-plants-vue/src/components/ProductCard.vue` to use `<fe-img>` instead of raw `<img>`
- [x] Write tests for fe-img (src loading state, error fallback, attribute reflection)

### 3.5.2. Add loader components

**Problem:** No loading state indicators during route transitions or async data fetches.

- [x] Create data loading loader component (shown while API calls / async composables are in-flight)
- [x] Decide component location: inside `packages/ui/` as `fe-loader` or in `apps/white-label-vue/src/components/` as Vue SFC
- [x] Integrate loaders into white-label app shell / layout
- [x] Ensure WA styling compatibility (use `--wa-*` design tokens)
- [x] Write/update tests

### 3.5.3. Improve fake-plants-vue Turborepo pipeline

- [x] Audit `turbo.json` for tenant-specific cache configuration
- [x] If tenant build differs from standard, add explicit pipeline task
- [x] Verify parallel task execution works for tenant build chain
- [x] ~~Add `.turbo` to tenant `.gitignore` if missing~~ — tenant `.gitignore` scaffolded, root `.gitignore` covers `.turbo` globally. Task B (vue-tsc typecheck + scoped cache inputs) deferred → see README "Next Future Improvements"

## ✅ Manual Confirmation

- [x] `bun run build` from root succeeds
- [x] `bun run test` from root succeeds
- [x] `cd apps/fake-plants-vue && bun run dev` — loaders visible during navigation
- [x] `cd apps/fake-plants-vue && bun run dev` — images show fallback on broken src
