<!-- Add 3.5.1 to improve fake plants image request (everytime is requesting the images) and custom img component having a default image in case of error
<!-- 3.5.2 Add a 2 loader component when we are navigating and when the resource is loading
| 3.5.1 | Improve fake plants image request and custom img component |
| 3.5.2 | Add a 2 loader component when we are navigating and when the resource is loading |
| 3.5.3 |  Improve fake plants Turborepo pipeline update -->
# 3.5. Turborepo Pipeline Update

**Goal:** Ensure tenant apps build correctly via Turborepo with proper dependency chain.

## Sources

| Source file               | Content         |
| ------------------------- | --------------- |
| `turbo.json`              | Pipeline config |
| `package.json` workspaces | `apps/*` glob   |

## Tasks

### 3.5.1. Verify workspace glob handles tenants

- [x] `package.json` workspaces: `["apps/*", "packages/*"]` — this already covers `apps/fake-plants-vue/` and `apps/*-vue` automatically
- [x] No turbo.json change needed — Turborepo auto-discovers workspaces

### 3.5.2. Verify dep chain

- [x] `fake-plants-vue` has `white-label-vue` in deps → turbo resolves `^build` dep chain automatically
- [x] Chain: `@repo/domain` → `@repo/infra` → `@repo/ui` → `@repo/presenters` → `white-label-vue` → `fake-plants-vue`
- [x] Run `bun run build` from root — verify all packages + apps build in correct order

### 3.5.3. Optional: Explicit pipeline for tenants

- [x] Only needed if tenant build differs from standard `vite build`
- [x] If keeping standard, no changes required

## ✅ Manual Confirmation

- [x] `bun run build` from root succeeds
- [x] `bun run test` from root succeeds
- [x] Human confirms: "Pipeline works, proceed to 3.6"
