# 3.5. Turborepo Pipeline Update

**Goal:** Ensure tenant apps build correctly via Turborepo with proper dependency chain.

## Sources

| Source file | Content |
|------------|---------|
| `turbo.json` | Pipeline config |
| `package.json` workspaces | `apps/*` glob |

## Tasks

### 3.5.1. Verify workspace glob handles tenants

- [x] `package.json` workspaces: `["apps/*", "packages/*"]` — this already covers `apps/fake-plants-vue/` and `apps/tenant-*` automatically
- [x] No turbo.json change needed — Turborepo auto-discovers workspaces

### 3.5.2. Verify dep chain

- [x] `fake-plants-vue` has `@repo/white-label-vue` in deps → turbo resolves `^build` dep chain automatically
- [x] Chain: `@repo/domain` → `@repo/infra` → `@repo/ui` → `@repo/presenters` → `@repo/white-label-vue` → `fake-plants-vue`
- [x] Run `bun run build` from root — verify all packages + apps build in correct order

### 3.5.3. Optional: Explicit pipeline for tenants

- [x] Only needed if tenant build differs from standard `vite build`
- [x] If keeping standard, no changes required

## ✅ Manual Confirmation

- [x] `bun run build` from root succeeds
- [x] `bun run test` from root succeeds
- [x] Human confirms: "Pipeline works, proceed to 3.6"
