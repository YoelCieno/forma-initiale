# 4.4. Angular Base Components — fe-* Compositing + CUSTOM_ELEMENTS_SCHEMA

**Status:** 🔶 IN PROGRESS

**Goal:** Wire white-label-angular's base components: WA styles + theme + tokens, CUSTOM_ELEMENTS_SCHEMA for `fe-*` tags, zoneless + signals + OnPush, and `fe-*` container components (Button/Card/Icon/Rating/ProductCard).

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/white-label-vue/src/components/*` | Vue fe-* container components to port |
| `apps/white-label-vue/src/styles/tokens.css` | Default brand tokens |
| `packages/ui/styles/themes/default.css` | WA theme (real CSS file importing WA `dist/styles/themes/default.css`) |
| `apps/white-label-vue/src/main.ts` | WA base + theme import order |
| `apps/white-label-vue/index.html` | Theme class on `<html>` |
| `.opencode/plans/core-foundation/phase-4/4.1-risks-mitigations.md` | R3 CSS imports, R8 zoneless, R10 schema tradeoff |

## Tasks

### 4.4.1. WA styles + theme + tokens (R3)

- [x] Import `@repo/ui/styles` (WA base) + `@repo/ui/styles/themes/<name>` (theme) + app `tokens.css`
- [x] Verify CSS side-effect imports survive `@angular/build` (fallback: angular.json `styles` array, or `@import` in `src/styles.css`)
- [x] Theme class statically on `<html>` in `src/index.html` (activates WA styling)

### 4.4.2. Theme files as CSS, not TS (vue apps)

- [x] Convert `packages/ui/styles/themes/*.ts` (default / awesome / shoelace) to real `.css` files instead of TS re-export shims
- [x] Each `.css` imports its WA source (e.g. `@awesome.me/webawesome/dist/styles/themes/default.css`) + any DS overrides
- [x] Update `packages/ui/package.json` exports: `./styles/themes/<name>` -> `./styles/themes/<name>.css`
- [x] Update vue apps (`white-label-vue`, `fake-plants-vue`) `main.ts` — import `@repo/ui/styles/themes/<name>` resolves to `.css` directly (no TS shim)
- [x] Verify Vite resolves the `.css` theme import and token cascade renders in built app

### 4.4.3. CUSTOM_ELEMENTS_SCHEMA (R10)

- [x] ~~Apply schema via base decorator factory `feComponent()`~~ **REMOVED** — AOT-incompatible (static analysis can't detect wrapper → JIT fallback → blank page). All components now use `@Component({ schemas: [CUSTOM_ELEMENTS_SCHEMA] })` directly. `factories/create-custom-elements.ts` deleted.
  - Rationale preserved: Angular `schemas` are per-component and NOT inherited — each component must declare `CUSTOM_ELEMENTS_SCHEMA` individually.
- [x] `fe-*` surface covers the 5 container components AND page templates that use `fe-async-content`/`fe-loader` raw (matching white-label-vue parity).
- [x] Export `Fe*Element` interfaces to recover a typed TS handle (ElementRef/ViewChild) for owned wrappers (schema still disables template-compiler checking).

### 4.4.4. Zoneless + signals (R8)

- [ ] `provideZonelessChangeDetection()` in shared factory config
- [ ] All view state via signals; components OnPush-compatible; no zone-CDD reliance

### 4.4.5. fe-* container components

- [ ] `Button` container (fe-button)
- [ ] `Card` container (fe-card)
- [ ] `Icon` container (fe-icon)
- [ ] `Rating` container (fe-rating)
- [ ] `ProductCard` container (fe-card + fe-img + fe-rating)
- [ ] Keep fe-* usage minimal — wrap in Angular components (R10 discipline)

### 4.4.6. Shared @repo/utils package (util unification)

- [ ] Create `@repo/utils` internal package; move shared type guards (`isFunction`, `isObject`) + other duplicated helpers out of `packages/generator`, `apps/white-label-vue`, `apps/white-label-angular/src/utils` into it.
- [ ] Consumers import from `@repo/utils` (single source of truth); delete local copies.

## Validation Checks

- [ ] WA styles render (base + theme + tokens) in built app (R3)
- [ ] `fe-*` tags render in Angular templates
- [ ] No `NG0203`/CD warnings; state reflects without zone.js (R8)
- [ ] `bunx ng test --watch=false` green
- [ ] `bun run build` from root green

## ✅ Manual Confirmation

- [ ] Human confirms: base components styled + zoneless-sound, proceed to 4.5
