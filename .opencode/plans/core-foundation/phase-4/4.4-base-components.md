# 4.4. Angular Base Components — fe-* Compositing + CUSTOM_ELEMENTS_SCHEMA

**Status:** 🔶 IN PROGRESS

**Goal:** Wire white-label-angular's base components: WA styles + theme + tokens, CUSTOM_ELEMENTS_SCHEMA for `fe-*` tags, zoneless + signals + OnPush, and `fe-*` container components (Button/Card/Icon/Rating/ProductCard).

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/white-label-vue/src/components/*` | Vue fe-* container components to port |
| `apps/white-label-vue/src/styles/tokens.css` | Default brand tokens |
| `apps/white-label-vue/src/styles/wa-theme.ts` | WA theme import |
| `apps/white-label-vue/src/main.ts` | WA base + theme import order |
| `apps/white-label-vue/index.html` | Theme class on `<html>` |
| `.opencode/plans/core-foundation/phase-4/4.1-risks-mitigations.md` | R3 CSS imports, R8 zoneless, R10 schema tradeoff |

## Tasks

### 4.4.1. WA styles + theme + tokens (R3)

- [ ] Import `@repo/ui/styles` (WA base) + `@repo/ui/styles/themes/<name>` (theme) + app `tokens.css` in `main.ts`
- [ ] Verify CSS side-effect imports survive `@angular/build` (fallback: angular.json `styles` array, or `@import` in `src/styles.css`)
- [ ] Theme class statically on `<html>` in `src/index.html` (activates WA styling)

### 4.4.2. CUSTOM_ELEMENTS_SCHEMA (R10)

- [ ] Add `CUSTOM_ELEMENTS_SCHEMA` to root App component + shared components (registers `fe-*` tags)
- [ ] Export `Fe*Element` interfaces to recover type safety (schema kills template typecheck)

### 4.4.3. Zoneless + signals (R8)

- [ ] `provideZonelessChangeDetection()` in shared factory config
- [ ] All view state via signals; components OnPush-compatible; no zone-CDD reliance

### 4.4.4. fe-* container components

- [ ] `Button` container (fe-button)
- [ ] `Card` container (fe-card)
- [ ] `Icon` container (fe-icon)
- [ ] `Rating` container (fe-rating)
- [ ] `ProductCard` container (fe-card + fe-img + fe-rating)
- [ ] Keep fe-* usage minimal — wrap in Angular components (R10 discipline)

## Validation Checks

- [ ] WA styles render (base + theme + tokens) in built app (R3)
- [ ] `fe-*` tags render in Angular templates
- [ ] No `NG0203`/CD warnings; state reflects without zone.js (R8)
- [ ] `bunx ng test --watch=false` green
- [ ] `bun run build` from root green

## ✅ Manual Confirmation

- [ ] Human confirms: base components styled + zoneless-sound, proceed to 4.5
