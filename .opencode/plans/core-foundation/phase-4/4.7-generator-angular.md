# 4.7. `@repo/generator` — Angular Tenant Generator

**Status:** ⏳ PENDING

**Goal:** Extend `@repo/generator` with an Angular tenant template mirroring `vue-tenant.tpl.ts`, so tenants can be scaffolded per-framework.

## Sources

| Source file | Content |
| ----------- | ------- |
| `packages/generator/src/templates/vue-tenant.tpl.ts` | Vue template to mirror |
| `packages/generator/src/*` | Pinion generator structure, CLI, register-tenant |
| `apps/fake-plants-angular/` | Reference tenant shape (from 4.6) |

## Tasks

### 4.7.1. Angular template

- [ ] `angular-tenant.tpl.ts` mirroring `vue-tenant.tpl.ts`
- [ ] Templates: `package.json`, `angular.json`, tsconfigs, `main.ts`, app shell, `tokens.css`, vitest config, env files (`environment*.ts`)

### 4.7.2. CLI wiring

- [ ] `generate:angular-tenant` root script (parallel to `generate:vue-tenant`)
- [ ] `register-tenant` Angular support

### 4.7.3. Tests + e2e

- [ ] Generator spec tests for `angular-tenant.tpl.ts`
- [ ] E2E: regenerate a test Angular tenant, verify build + test green

## Validation Checks

- [ ] `bun run generate:angular-tenant` scaffolds a working tenant
- [ ] Regenerated tenant builds (`bun run build`) + tests green (`ng test`)
- [ ] Generator spec tests pass
- [ ] No duplication — generated tenant extends white-label-angular (workspace dep)

## ✅ Manual Confirmation

- [ ] Human confirms: generator emits a working Angular tenant, proceed to 4.8
