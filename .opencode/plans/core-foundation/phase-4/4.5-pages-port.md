# 4.5. Port Pages — Products, Components, AppShell, metaMap

**Status:** ⏳ PENDING

**Goal:** Port white-label-vue's pages (Products, Components), AppShell navigation, and metadata injection into Angular, filling the routes defined in 4.3.

## Sources

| Source file                                         | Content                                     |
| --------------------------------------------------- | ------------------------------------------- |
| `apps/white-label-vue/src/pages/ProductsPage.vue`   | Products page to port                       |
| `apps/white-label-vue/src/pages/ComponentsPage.vue` | Components page to port                     |
| `apps/white-label-vue/src/components/AppShell.vue`  | Shell/nav to port                           |
| `apps/white-label-vue/src/App.vue`                  | Root layout to port                         |
| `@repo/presenters`                                  | View-model transforms (`toProductViewList`) |

## Tasks

### 4.5.1. ProductsPage

- [x] Review Angular `ProductsPage` using `fe-async-content` / `fe-loader` for async product loading
- [x] Review Wire `@repo/infra` fetch adapters + `@repo/presenters` view-model transforms
- [x] Spec tests

### 4.5.2. ComponentsPage

- [x] Review Angular `ComponentsPage` showcasing fe-\* components
- [x] Spec tests

### 4.5.3. AppShell

- [x] Angular `AppShell` with nav (Products / Components)
- [x] Routes wired to `/` (products) + `/components`

### 4.5.4. metaMap via META_MAP_INJECTION_KEY

- [x] `META_MAP_INJECTION_KEY` (Angular InjectionToken) provides framework metadata
- [x] Default frameworkMap provided at factory layer, overridable by tenants

### 4.5.5. Routes fill

- [x] `src/routes.ts` filled with ported pages (connects to 4.3 factory)
- [x] Hash-based routing matches Vue behavior

### 4.5.6. MetaMap — create shared metadata

- [x] Move `metadata.ts` to shared location (e.g. `packages/infra/src/metadata/{tenant(without_fw)}.ts`) so both Vue and Angular consume same source
- [x] Update `apps/white-label-vue/src/main.ts` import path to shared location
- [x] Create `apps/white-label-angular/metadata.ts` (or import from shared) and pass `metaMap: frameworkMap` in `apps/white-label-angular/src/main.ts:9` (currently missing)
- [x] Verify `ProductsPage` receives metaMap (remove `{ optional: true }` fallback or confirm correct behavior)

### 4.5.7. Sync styles content (tokens.css + base.css)

- [x] Copy full `apps/white-label-vue/src/styles/tokens.css` content (63 lines) into `apps/white-label-angular/src/styles/tokens.css` (currently 13 lines — missing semantic colors, layout, font scale, border radius)
- [x] Copy full `apps/white-label-vue/src/styles/base.css` content (15 lines) into `apps/white-label-angular/src/styles/base.css` (currently 3 lines — missing h2, .subheading**h3, .card**h4)
- [x] Verify `bun run build` for Angular still passes

## Validation Checks

- [x] `/` renders products via fe-async-content/fe-loader WITH metaMap applied
- [x] `/components` renders component showcase
- [x] Nav works between both routes
- [x] Presenters produce correct view models (frameworkMap applied)
- [x] Angular tokens.css and base.css match Vue content
- [x] All fe-\* properties use [prop]="value" (no bare attributes) — done in 4.4.5
- [x] `bunx ng test --watch=false` green; `bun run build` green

## ✅ Manual Confirmation

- [x] Human confirms: pages render + navigate correctly in browser, proceed to 4.6
