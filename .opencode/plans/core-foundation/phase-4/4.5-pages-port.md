# 4.5. Port Pages — Products, Components, AppShell, metaMap

**Status:** ⏳ PENDING

**Goal:** Port white-label-vue's pages (Products, Components), AppShell navigation, and metadata injection into Angular, filling the routes defined in 4.3.

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/white-label-vue/src/pages/ProductsPage.vue` | Products page to port |
| `apps/white-label-vue/src/pages/ComponentsPage.vue` | Components page to port |
| `apps/white-label-vue/src/components/AppShell.vue` | Shell/nav to port |
| `apps/white-label-vue/src/App.vue` | Root layout to port |
| `@repo/presenters` | View-model transforms (`toProductViewList`) |

## Tasks

### 4.5.1. ProductsPage

- [ ] Angular `ProductsPage` using `fe-async-content` / `fe-loader` for async product loading
- [ ] Wire `@repo/infra` fetch adapters + `@repo/presenters` view-model transforms
- [ ] Spec tests

### 4.5.2. ComponentsPage

- [ ] Angular `ComponentsPage` showcasing fe-* components
- [ ] Spec tests

### 4.5.3. AppShell

- [ ] Angular `AppShell` with nav (Products / Components)
- [ ] Routes wired to `/` (products) + `/components`

### 4.5.4. metaMap via META_MAP_INJECTION_KEY

- [ ] `META_MAP_INJECTION_KEY` (Angular InjectionToken) provides framework metadata
- [ ] Default frameworkMap provided at factory layer, overridable by tenants

### 4.5.5. Routes fill

- [ ] `src/routes.ts` filled with ported pages (connects to 4.3 factory)
- [ ] Hash-based routing matches Vue behavior

## Validation Checks

- [ ] `/` renders products via fe-async-content/fe-loader
- [ ] `/components` renders component showcase
- [ ] Nav works between both routes
- [ ] Presenters produce correct view models (frameworkMap applied)
- [ ] `bunx ng test --watch=false` green; `bun run build` green

## ✅ Manual Confirmation

- [ ] Human confirms: pages render + navigate correctly in browser, proceed to 4.6
