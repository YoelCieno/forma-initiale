# 4.3. Angular Factory — `createWhiteLabelApp` Equivalent

**Status:** ✅ COMPLETED

**Goal:** Implement the Angular layer factory, the parallel of white-label-vue's `createWhiteLabelApp`, returning `Promise<{ root, config }>` and bootstrapping via `bootstrapApplication`.

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/white-label-vue/src/bootstrap/app.ts` | `createWhiteLabelApp()` factory to port |
| `apps/white-label-vue/src/bootstrap/init.ts` | Merge/init logic to port |
| `apps/white-label-vue/src/routes.ts` | Default route array to port |
| `.opencode/plans/core-foundation/phase-4/4.1-risks-mitigations.md` | R1/R3/R8 drive bootstrap + mocks decisions |

## Tasks

### 4.3.1. `src/bootstrap/init.ts`

- [x] Port merge/init logic from Vue init.ts (options + merge)

### 4.3.2. `src/bootstrap/app.ts`

- [x] `createWhiteLabelApp(options)` factory — returns `Promise<{ root, config }>`
  - `root`: `ComponentRef<WhiteLabelRoot>` via `createComponent`
  - `config`: merged `WhiteLabelConfig` (routes, metaMap)

### 4.3.3. `src/routes.ts`

- [x] Default route array (mirrors white-label-vue)

### 4.3.4. `src/main.ts`

- [x] Standalone entry: `createWhiteLabelApp({})` → `bootstrapApplication(rootComponent, { providers })`

### 4.3.5. Options

- [x] `routes` — tenant-provided base routes
- [x] `extendRoutes` — tenant additions (merge at bootstrap, tenant wins on path)
- [x] `omitRoutePaths` — remove white-label default routes
- [x] `metaMap` — view-model metadata (provided via `META_MAP_INJECTION_KEY`)

### 4.3.6. Mocks gating (fileReplacements)

- [x] `environment.ts` / `environment.development.ts` swapped via angular.json `fileReplacements` gates MSW mocks

## Remaining notes (unchecked — carry to 4.4 / 4.5)

- [ ] `@repo/ui/styles` + theme + tokens import (R3) — 4.4
- [ ] CUSTOM_ELEMENTS_SCHEMA registration for fe-* tags (R10) — 4.4
- [ ] `provideZonelessChangeDetection()` in shared factory config (R8) — 4.4
- [ ] Routes filled with pages — 4.5

## Validation Checks

- [x] Spec tests cover `mergeRoutes` / `setupMocks` / `metaMap` (factory logic)
- [x] `bunx ng test --watch=false` — 21 tests green
- [x] `bun run build` from root — turbo build green
- [x] Repo-wide `bun run test` — turbo green (7 tasks)

## ✅ Manual Confirmation

- [x] Human confirms: factory verified (tests + build green), proceed to 4.4
