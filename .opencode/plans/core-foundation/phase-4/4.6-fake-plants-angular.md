# 4.6. `fake-plants-angular` — Example Tenant (Manual Parity Check)

**Status:** ⏳ PENDING — NOT optional. Manual parity check vs `fake-plants-vue` is required before building the generator (4.7).

**Goal:** Create the Angular tenant manually to validate the Angular layer mechanism end-to-end (parity with `fake-plants-vue`), before building the Angular generator.

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/fake-plants-vue/` | Tenant to mirror (extendRoutes, omitRoutePaths, appShell, metaMap, tokens) |
| `apps/white-label-angular/` | Angular layer base to extend |
| `apps/white-label-angular/src/bootstrap/app.ts` | `createWhiteLabelApp()` factory |
| `.opencode/plans/core-foundation/phase-4/4.3-factory.md` | Factory options (routes/extendRoutes/omitRoutePaths/metaMap) |

## Tasks

### 4.6.1. Tenant app

- [ ] `mkdir -p apps/fake-plants-angular` — tenant consuming `white-label-angular` as workspace dep (no `@repo` scope)
- [ ] `package.json` + `angular.json` + tsconfigs

### 4.6.2. Route overrides

- [ ] `extendRoutes` `/about` (AboutPage tenant-specific)
- [ ] `omitRoutePaths` `['/components']` (hide white-label Components page, matching fake-plants-vue)

### 4.6.3. AppShell override

- [ ] Tenant root component overrides white-label shell — nav: Products / About (no separate `appShell` option)

### 4.6.4. metaMap override

- [ ] `plantsMap` metaMap (7 plants) provided via `META_MAP_INJECTION_KEY`, overriding white-label default

### 4.6.5. Brand + components

- [ ] Green brand `tokens.css` (matches fake-plants-vue green overrides)
- [ ] `ProductCard` override (plant icons via fe-icon, different layout)

### 4.6.6. MSW + register-tenant

- [ ] MSW mocks wired; tenant registered via `register-tenant` (`--prefix` / `--names-json`)

## Validation Checks

- [ ] **Style override:** green brand tokens visible
- [ ] **Component override:** ProductCard renders with plant icons, not white-label default
- [ ] **New component:** AboutPage renders at `/about`
- [ ] **Route omit:** `/components` hidden
- [ ] **metaMap:** 7 plants applied
- [ ] **Layer fallback:** white-label components render as defaults where not overridden
- [ ] **No file duplication:** white-label unchanged, tenant only holds diffs
- [ ] `bunx ng test --watch=false` green; `bun run build` green

## ✅ Manual Confirmation

- [ ] Human visually inspects: manual visual parity vs `fake-plants-vue` (styles, components, routes, metaMap)
- [ ] Human confirms: "Angular layer mechanism validated, proceed to 4.7"
