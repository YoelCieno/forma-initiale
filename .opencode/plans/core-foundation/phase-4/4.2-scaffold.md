# 4.2. Scaffold `apps/white-label-angular/` — Angular CLI 22

**Status:** ✅ COMPLETED

**Goal:** Scaffold the Angular layer-base app consuming `@repo/*` workspace packages, following the same hexagonal architecture as white-label-vue.

## Sources

| Source file | Content |
| ----------- | ------- |
| `apps/white-label-vue/` | Layer base to mirror (structure, deps, MSW) |
| `.opencode/plans/core-foundation/phase-4/4.1-risks-mitigations.md` | Risk register driving scaffold decisions (R2 paths, R6 TS pin, R7 vitest/MSW, R9 skip-install) |

## Tasks

### 4.2.1. Scaffold with Angular CLI 22

- [x] `bunx @angular/cli@22 new white-label-angular --skip-install` (or equivalent) — bare Angular CLI 22, no AnalogJS (R9: always `--skip-install`)
- [x] Configured zoneless (v22 default), standalone components

### 4.2.2. Workspace deps

- [x] Add workspace deps to `package.json`: `@repo/domain`, `@repo/infra`, `@repo/presenters`, `@repo/ui`

### 4.2.3. TypeScript pin (R6)

- [x] Pin `typescript: ~6.0.2` in app devDependencies (peer `>=6.0 <6.1`), avoid drift to `<6.1`

### 4.2.4. `@repo/*` paths (R2)

- [x] `tsconfig.app.json` paths mapping for each `@repo/*` → concrete TS source file (`module: preserve` + `moduleResolution: bundler`)
- [x] Tenant alias mapping (`white-label-angular/app` + `src/*`) for future tenants

### 4.2.5. `angular.json` fileReplacements

- [x] `fileReplacements` configured to swap `src/environments/environment.ts` → `environment.development.ts` for dev/test builds (mocks gating)

### 4.2.6. Environments

- [x] `src/environments/environment.ts` + `src/environments/environment.development.ts` (mocks flag)

### 4.2.7. Vitest + jsdom (R7)

- [x] `tsconfig.spec.json` + `jsdom@^28` + `vitest@^4.0.8`
- [x] Port MSW `setupMocks` into spec setup, mirroring white-label-vue `vitest.setup.ts`

### 4.2.8. Dev server dep-optimizer fix (2026-08-15)

- [x] `--prebundle=false` baked into `package.json` dev script + `angular.json` serve.options (workaround: `@angular/build` rolldown optimizer can't resolve `.ts` in `packages/infra/src/mocks/browser.ts`)

### 4.2.9. MSW runtime

- [x] Copied `mockServiceWorker.js` from white-label-vue into `apps/white-label-angular/public/` (needed for MSW runtime)

## Validation Checks

- [x] `bunx ng test --watch=false` — 21 tests green (3 files)
- [x] `bun run build` from root — turbo build green
- [x] `bun run lint` — eslint exit 0
- [x] `bunx ng serve` — dev server boots (prebundle fix holds)

## ✅ Manual Confirmation

- [x] Human confirms: scaffold verified (test/build/lint green), proceed to 4.3
