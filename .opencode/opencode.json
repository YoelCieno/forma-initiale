{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "fix": {
      "description": "Clean up technical debt from the last task — WA attr reflection, hybridJS timing, missing exports, stale decls, config drift",
      "template": "{file:commands/fix.md}\n\n$ARGUMENTS",
      "agent": "debt-cleanup",
      "subtask": true
    },
    "improve": {
      "description": "Clean up technical debt from the last task — alias for /fix",
      "template": "{file:commands/fix.md}\n\n$ARGUMENTS",
      "agent": "debt-cleanup",
      "subtask": true
    }
  },
  "agent": {
    "debt-cleanup": {
      "description": "Fixes technical debt from completed tasks — WA attr reflection, hybridJS render timing, missing package exports, stale auto-imports, config drift, untracked files.",
      "mode": "subagent",
      "permission": {
        "edit": "allow",
        "bash": "allow"
      }
    }
  }
}
